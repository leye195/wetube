extends ./layouts/main.pug
block content
    .video__player
        video(src=`/${video.fileUrl}`,controls=true)
    .video__info
        a(href=routes.editVideo(video._id)) Edit Video
        h1.video__title=video.title
        p.video__description=video.description
        p.video__view=video.view